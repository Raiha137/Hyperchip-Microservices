    spring.application.name=Hyperchip-order
    server.port=8092


    # Database
    spring.datasource.url=jdbc:mysql://localhost:3306/hyperchip_order_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    spring.datasource.username=root
    spring.datasource.password=Fathima@137
    spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

    # Config Server client (match User service)
    spring.cloud.config.uri=http://localhost:8888

spring.cloud.config.fail-fast=false
    spring.config.import=optional:configserver:http://localhost:8888


    spring.jpa.hibernate.ddl-auto=update
    spring.jpa.show-sql=true
    spring.jpa.properties.hibernate.format_sql=true
    spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect


    # Eureka (optional if you use Eureka)
    eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
    eureka.client.register-with-eureka=true
    eureka.client.fetch-registry=true

    # email fallback for order confirmations (used only if order.userEmail missing)
    order.confirm.recipient=hyperchip.team@gmail.com


    # Gmail SMTP (use environment variables - DO NOT commit real credentials)
    spring.mail.host=smtp.gmail.com
    spring.mail.port=587
    spring.mail.username=fraiha137@gmail.com
    spring.mail.password=xrksbopsyusfnzvp
    spring.mail.properties.mail.smtp.auth=true
    spring.mail.properties.mail.smtp.starttls.enable=true
    spring.mail.properties.mail.smtp.starttls.required=true
    spring.mail.default-encoding=UTF-8
    spring.mail.properties.mail.smtp.connectiontimeout=5000
    spring.mail.properties.mail.smtp.timeout=5000
    spring.mail.properties.mail.smtp.writetimeout=5000
    spring.mail.properties.mail.debug=true


    # application.properties
user.service.url=http://localhost:8083
# keep existing properties intact
spring.profiles.active=local



order.service.base-url=http://localhost:8092

uploads.base.url=http://localhost:8086
product.service.url=http://localhost:8086
master.service.url=http://localhost:8086

order.service.url=http://localhost:8092
payment.service.url=http://localhost:8094
cart.service.url=http://localhost:8091/api/cart
wallet.service.url=http://localhost:8095




# NOTE: if you previously had "spring.profiles.active=local", either remove it or ensure
# that the active profile allows the real MailService bean to be active.
# spring.profiles.active=local



management.endpoints.web.exposure.include=health 
